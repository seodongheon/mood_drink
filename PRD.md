1. 목표와 성공 조건
목표: 사용자에게 위로 멘트와 함께 상황에 맞는 주종과 안주를 추천해준다.

성공 조건: 사용자가 텍스트를 입력하면 AI가 추천하는 주종 1개, 안주 1개, 위로/공감 멘트가 화면에 출력되어야 한다.

2. 사용자 스토리
사용자: 코딩 과제, 헬스, 러닝 등으로 에너지가 완전히 방전되어 무언가를 결정하기 힘들때 고민할 필요 없이 빠르게 내 상황에 맞는 맞춤형 술상을 추천받고 싶다.

3. 화면 명세

입력창 ("오늘 하루 어떤 일이 있었는지, 기분이 어떤지 편하게 적어주세요.")

'추천받기' 액션 버튼: 버튼 클릭 시 버튼 텍스트가 '고민 중...'으로 변경되거나 간단한 스피너가 표시됨.
결과 카드 영역 (기본 상태: 숨김):

결과 반환 시 입력창 바로 아래에 노출.

출력 요소: 위로/공감 멘트 텍스트, 주종 텍스트, 안주 텍스트.

안내 문구: 입력이 비었을 때만 빨간 글씨로 표시된다.

4. 기능 명세
입력: 사용자가 텍스트를 붙여넣는다

검증: 비었는지, 너무 짧았는지 확인.

처리 (Process):

프롬프트는 AI가 사전 지식만을 활용하여 반드시 주종 1개, 안주 1개, 1~2문장의 공감 멘트형식을 반환하도록 강제한다.

출력 (Output): 결과를 화면에 표시한다

5. 예외 처리

입력이 비어 있으면?
API 호출을 차단하고, 텍스트 입력창 테두리를 붉은색으로 강조하거나 "오늘 하루를 짧게라도 들려주세요!"라는 경고 메시지를 입력창 하단에 띄워 입력을 유도합니다.

너무 짧거나 너무 길면?

너무 길면: 텍스트 입력창에 최대 글자 수(예: 300자) 제한을 설정하여 물리적으로 입력을 막거나, 초과된 텍스트를 자동으로 잘라내고(Truncate) API를 호출합니다.

너무 짧으면: PRD 상에 빈칸(0자)에 대한 방어 로직은 있으나, 최소 글자 수(예: 5자 미만) 제한이 필요하다면 빈칸 입력과 동일하게 "조금 더 자세히 들려주세요!"라는 경고 멘트를 추가해 예외 처리를 보완할 수 있습니다.

AI 응답이 실패하면?
사용자의 네트워크 문제나 AI API 서버 오류(5xx 에러)로 인해 응답에 실패할 경우, "일시적인 연결 문제가 발생했습니다. 잠시 후 다시 시도해 주세요."라는 알림(Toast 또는 텍스트)을 띄우고 '추천받기' 버튼을 다시 활성화하여 재시도할 수 있게 합니다.

응답이 오래 걸리면?
성공 기준인 3초를 초과하면 무한 로딩을 방지하기 위해 진행 중인 API 요청을 즉시 강제 중단(Timeout)합니다. 그 후 "AI가 너무 깊게 고민하네요! 오늘은 무조건 시원한 맥주와 치킨을 추천합니다."와 같이 미리 준비된 기본값(Fallback) 추천 결과를 화면에 바로 띄워줍니다.

결과가 이상하면 다시 할 수 있나?
네, 가능합니다.

UI 보호: AI가 정해진 형식(주종, 안주, 멘트)을 어기고 이상한 줄글을 반환해 파싱 에러가 발생하더라도, 즉시 화면에 '기본값(Fallback)' 결과를 대신 렌더링하므로 화면이 깨지지 않습니다.

재시도 가능: 결과를 출력한 후에는 항상 '추천받기' 버튼이 다시 활성화되므로, 사용자는 내용이 마음에 들지 않거나 결과가 이상할 경우 언제든 버튼을 눌러 새롭게 추천을 받을 수 있습니다.

6. 완료 조건
입력하고 버튼을 누르면 결과가 나온다
빈 입력이면 안내 문구가 뜬다
처리 중에 로딩 표시가 보인다
실패해도 화면이 죽지 않는다
